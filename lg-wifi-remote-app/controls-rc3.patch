diff --git a/app/build.gradle.kts b/app/build.gradle.kts
index c4137cb..455eff9 100644
--- a/app/build.gradle.kts
+++ b/app/build.gradle.kts
@@ -12,8 +12,8 @@ android {
         applicationId = "io.github.grupogptespecialeng.libreremote"
         minSdk = 23
         targetSdk = 35
-        versionCode = 21
-        versionName = "2.0.0-rc2-stabilized"
+        versionCode = 22
+        versionName = "2.0.0-rc3-controls"
         vectorDrawables.useSupportLibrary = true
     }
 
diff --git a/app/src/main/java/com/geronimo/controlelgwifi/FastLgWebOsClient.kt b/app/src/main/java/com/geronimo/controlelgwifi/FastLgWebOsClient.kt
index 69b9059..be2f548 100644
--- a/app/src/main/java/com/geronimo/controlelgwifi/FastLgWebOsClient.kt
+++ b/app/src/main/java/com/geronimo/controlelgwifi/FastLgWebOsClient.kt
@@ -79,7 +79,7 @@ class FastLgWebOsClient(
     init {
         scope.launch {
             for (motion in motionChannel) {
-                sendPointerNow("type:move\ndx:${motion.dx}\ndy:${motion.dy}\ndown:0\n\n")
+                sendPointer("type:move\ndx:${motion.dx}\ndy:${motion.dy}\ndown:0\n\n")
                 delay(12)
             }
         }
@@ -401,12 +401,15 @@ class FastLgWebOsClient(
         ensurePointerSocket()
     }
 
-    private fun sendPointerNow(message: String) {
-        if (pointerReady) pointerSocket?.send(message)
-    }
-
     private fun ensurePointerSocket() {
         if (!registered || pointerReady || !pointerOpening.compareAndSet(false, true)) return
+        val openingGeneration = connectionGeneration
+        scope.launch {
+            delay(4_500)
+            if (openingGeneration == connectionGeneration && !pointerReady) {
+                pointerOpening.set(false)
+            }
+        }
         request("ssap://com.webos.service.networkinput/getPointerInputSocket", timeoutMs = 4_000, callback = { payload ->
             val path = payload.optString("socketPath")
             if (path.isBlank()) {
diff --git a/app/src/main/java/com/geronimo/controlelgwifi/MainActivity.kt b/app/src/main/java/com/geronimo/controlelgwifi/MainActivity.kt
index 82a8c8f..8dcc5d3 100644
--- a/app/src/main/java/com/geronimo/controlelgwifi/MainActivity.kt
+++ b/app/src/main/java/com/geronimo/controlelgwifi/MainActivity.kt
@@ -498,6 +498,26 @@ private fun RemotePresetContent(state: RemoteUiState, viewModel: RemoteViewModel
                     }
                 }
             }
+            if (RemoteModule.TouchpadShortcut in modules && TvCapability.Pointer in state.capabilities) {
+                item {
+                    Surface(
+                        onClick = { viewModel.setControlSurface(ControlSurface.Touchpad) },
+                        modifier = Modifier.fillMaxWidth().height(54.dp),
+                        shape = RoundedCornerShape(17.dp),
+                        color = MaterialTheme.colorScheme.primaryContainer
+                    ) {
+                        Row(
+                            Modifier.padding(horizontal = 14.dp),
+                            verticalAlignment = Alignment.CenterVertically,
+                            horizontalArrangement = Arrangement.Center
+                        ) {
+                            Icon(Icons.Rounded.TouchApp, contentDescription = null)
+                            Spacer(Modifier.width(7.dp))
+                            Text("Abrir mousepad / touchpad", fontWeight = FontWeight.SemiBold)
+                        }
+                    }
+                }
+            }
             item { Spacer(Modifier.height(14.dp)) }
         }
     }
diff --git a/app/src/main/java/com/geronimo/controlelgwifi/RemoteControls.kt b/app/src/main/java/com/geronimo/controlelgwifi/RemoteControls.kt
index a43b338..480ba6e 100644
--- a/app/src/main/java/com/geronimo/controlelgwifi/RemoteControls.kt
+++ b/app/src/main/java/com/geronimo/controlelgwifi/RemoteControls.kt
@@ -6,7 +6,6 @@ import androidx.compose.foundation.border
 import androidx.compose.foundation.gestures.awaitEachGesture
 import androidx.compose.foundation.gestures.awaitFirstDown
 import androidx.compose.foundation.gestures.detectDragGestures
-import androidx.compose.foundation.gestures.detectTapGestures
 import androidx.compose.foundation.layout.Arrangement
 import androidx.compose.foundation.layout.Box
 import androidx.compose.foundation.layout.Column
@@ -70,6 +69,7 @@ import androidx.compose.ui.text.style.TextAlign
 import androidx.compose.ui.unit.Dp
 import androidx.compose.ui.unit.dp
 import androidx.compose.ui.unit.sp
+import kotlin.math.abs
 import kotlin.math.roundToInt
 
 @Composable
@@ -116,16 +116,19 @@ fun PressControl(
                 .pointerInput(enabled, haptics) {
                     if (!enabled) return@pointerInput
                     awaitEachGesture {
-                            awaitFirstDown(requireUnconsumed = false)
-                            pressed = true
-                            if (haptics) haptic.performHapticFeedback(HapticFeedbackType.TextHandleMove)
-                            onPress()
+                        awaitFirstDown(requireUnconsumed = false)
+                        pressed = true
+                        if (haptics) haptic.performHapticFeedback(HapticFeedbackType.TextHandleMove)
+                        onPress()
+                        try {
                             do {
                                 val event = awaitPointerEvent()
                                 val stillPressed = event.changes.any { it.pressed }
                             } while (stillPressed)
+                        } finally {
                             pressed = false
-                        onRelease()
+                            onRelease()
+                        }
                     }
                 },
             shape = CircleShape,
@@ -171,9 +174,9 @@ fun RemoteDPad(
     compact: Boolean,
     onAction: (RemoteAction) -> Unit
 ) {
-    val outer = if (compact) 218.dp else 244.dp
+    val outer = if (compact) 244.dp else 276.dp
     val direction = if (compact) 60.dp else 68.dp
-    val center = if (compact) 78.dp else 88.dp
+    val center = if (compact) 74.dp else 82.dp
     Card(
         modifier = Modifier.size(outer),
         shape = CircleShape,
@@ -522,20 +525,36 @@ fun TouchpadSurface(state: RemoteUiState, viewModel: RemoteViewModel) {
                 .border(1.dp, MaterialTheme.colorScheme.outline.copy(alpha = 0.25f), RoundedCornerShape(30.dp))
                 .pointerInput(state.connected) {
                     if (!state.connected) return@pointerInput
-                    detectTapGestures(onTap = { viewModel.clickPointer() })
-                }
-                .pointerInput(state.connected) {
-                    if (!state.connected) return@pointerInput
-                    detectDragGestures(
-                        onDragStart = { dragging = true },
-                        onDragEnd = { dragging = false },
-                        onDragCancel = { dragging = false }
-                    ) { change, amount ->
-                        change.consume()
-                        viewModel.movePointer(
-                            (amount.x * 1.65f).roundToInt(),
-                            (amount.y * 1.65f).roundToInt()
-                        )
+                    awaitEachGesture {
+                        val down = awaitFirstDown(requireUnconsumed = false)
+                        var totalDrag = 0f
+                        var pastSlop = false
+                        dragging = false
+                        try {
+                            var fingerDown = true
+                            while (fingerDown) {
+                                val event = awaitPointerEvent()
+                                val change = event.changes.firstOrNull { it.id == down.id } ?: break
+                                val dx = change.position.x - change.previousPosition.x
+                                val dy = change.position.y - change.previousPosition.y
+                                totalDrag += abs(dx) + abs(dy)
+                                if (!pastSlop && totalDrag >= viewConfiguration.touchSlop) {
+                                    pastSlop = true
+                                    dragging = true
+                                }
+                                if (pastSlop && (dx != 0f || dy != 0f)) {
+                                    change.consume()
+                                    viewModel.movePointer(
+                                        (dx * 1.65f).roundToInt(),
+                                        (dy * 1.65f).roundToInt()
+                                    )
+                                }
+                                fingerDown = change.pressed
+                            }
+                        } finally {
+                            dragging = false
+                        }
+                        if (!pastSlop) viewModel.clickPointer()
                     }
                 },
             contentAlignment = Alignment.Center
@@ -573,7 +592,7 @@ fun TouchpadSurface(state: RemoteUiState, viewModel: RemoteViewModel) {
                         if (!state.connected) return@pointerInput
                         detectDragGestures { change, amount ->
                             change.consume()
-                            viewModel.scrollPointer((amount.y * 0.35f).roundToInt())
+                            viewModel.scrollPointer((amount.y * 1.25f).roundToInt())
                         }
                     },
                 shape = RoundedCornerShape(18.dp),
diff --git a/app/src/main/java/com/geronimo/controlelgwifi/RemoteModels.kt b/app/src/main/java/com/geronimo/controlelgwifi/RemoteModels.kt
index a36727b..4d4e4e8 100644
--- a/app/src/main/java/com/geronimo/controlelgwifi/RemoteModels.kt
+++ b/app/src/main/java/com/geronimo/controlelgwifi/RemoteModels.kt
@@ -123,6 +123,7 @@ enum class RemotePresetId(val title: String) {
     Simple("Simples"),
     Normal("Normal"),
     Advanced("Avançado"),
+    AdvancedLegacy("Avançado AL"),
     Custom1("Personalizado 1"),
     Custom2("Personalizado 2"),
     Custom3("Personalizado 3");
@@ -184,10 +185,31 @@ data class RemotePreset(
             modules = RemoteModule.entries.toList(),
             compact = true
         )
+        val advancedLegacy = RemotePreset(
+            RemotePresetId.AdvancedLegacy,
+            name = "Avançado AL",
+            modules = listOf(
+                RemoteModule.Power,
+                RemoteModule.DPad,
+                RemoteModule.Volume,
+                RemoteModule.Channels,
+                RemoteModule.CoreActions,
+                RemoteModule.Media,
+                RemoteModule.Apps,
+                RemoteModule.Inputs,
+                RemoteModule.Keyboard,
+                RemoteModule.Colors,
+                RemoteModule.InfoMenu,
+                RemoteModule.Numeric,
+                RemoteModule.TouchpadShortcut
+            ),
+            compact = false
+        )
         fun defaultFor(id: RemotePresetId): RemotePreset = when (id) {
             RemotePresetId.Simple -> simple
             RemotePresetId.Normal -> normal
             RemotePresetId.Advanced -> advanced
+            RemotePresetId.AdvancedLegacy -> advancedLegacy
             else -> normal.copy(id = id, name = id.title)
         }
     }
diff --git a/app/src/test/java/com/geronimo/controlelgwifi/RemoteModelsTest.kt b/app/src/test/java/com/geronimo/controlelgwifi/RemoteModelsTest.kt
index 303d52f..fed18c6 100644
--- a/app/src/test/java/com/geronimo/controlelgwifi/RemoteModelsTest.kt
+++ b/app/src/test/java/com/geronimo/controlelgwifi/RemoteModelsTest.kt
@@ -102,4 +102,16 @@ class RemoteModelsTest {
         assertEquals(null, TvDiscovery.normalizeHost("8.8.8.8"))
     }
 
+
+
+    @Test
+    fun advancedLegacyPresetKeepsClassicControlsInOnePreset() {
+        val modules = RemotePreset.advancedLegacy.modules
+        assertTrue(RemoteModule.DPad in modules)
+        assertTrue(RemoteModule.Volume in modules)
+        assertTrue(RemoteModule.Media in modules)
+        assertTrue(RemoteModule.Numeric in modules)
+        assertTrue(RemoteModule.TouchpadShortcut in modules)
+    }
+
 }
