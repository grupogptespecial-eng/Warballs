{
  "name": "warballs",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "npm run test:syntax && npm run test:scope",
    "test:syntax": "find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test:scope": "node scripts/check-repository-scope.mjs"
  }
}
