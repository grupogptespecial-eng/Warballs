diff --git a/app/src/main/java/com/geronimo/controlelgwifi/TvBackend.kt b/app/src/main/java/com/geronimo/controlelgwifi/TvBackend.kt
index e0a35e3..b705331 100644
--- a/app/src/main/java/com/geronimo/controlelgwifi/TvBackend.kt
+++ b/app/src/main/java/com/geronimo/controlelgwifi/TvBackend.kt
@@ -23,6 +23,7 @@ import java.net.URLEncoder
 import java.nio.charset.StandardCharsets
 import java.security.MessageDigest
 import java.security.SecureRandom
+import java.security.cert.CertificateException
 import java.security.cert.X509Certificate
 import java.util.ArrayDeque
 import java.util.concurrent.TimeUnit
@@ -145,7 +146,13 @@ class LgWebOsBackend(
     )
     override val defaultCapabilities: Set<TvCapability> = TvCapability.lgDefaults
 
-    override fun connect(device: TvDevice, userInitiated: Boolean) = client.connect(device, userInitiated)
+    override fun connect(device: TvDevice, userInitiated: Boolean) {
+        if (!NetworkAddressValidator.isLocalHost(device.ip)) {
+            listener.onConnectionState(ConnectionState.Error, "O endereço da TV precisa pertencer à rede local")
+            return
+        }
+        client.connect(device, userInitiated)
+    }
     override fun close() = client.close()
     override fun forgetDevice(device: TvDevice) = client.forgetDevice(device.ip)
     override fun send(action: RemoteAction) {
@@ -222,6 +229,10 @@ class SamsungTizenBackend(
     override val defaultCapabilities: Set<TvCapability> = TvCapability.samsungDefaults
 
     override fun connect(device: TvDevice, userInitiated: Boolean) {
+        if (!NetworkAddressValidator.isLocalHost(device.ip)) {
+            listener.onConnectionState(ConnectionState.Error, "O endereço da TV precisa pertencer à rede local")
+            return
+        }
         if (userInitiated) reconnectAttempt = 0
         this.device = device
         manuallyClosed = false
@@ -266,15 +277,6 @@ class SamsungTizenBackend(
                     webSocket.close(1000, "Another endpoint won")
                     return
                 }
-                if (endpoint.startsWith("wss://") && !verifyOrStoreCertificate(response)) {
-                    endpointChosen.set(false)
-                    webSocket.close(1008, "Certificate changed")
-                    listener.onConnectionState(
-                        ConnectionState.Error,
-                        "A identidade segura da TV mudou. Remova a TV e pareie novamente."
-                    )
-                    return
-                }
                 socket = webSocket
                 device?.let { securePreferences.edit().putString("endpoint_${it.stableId}", endpoint).apply() }
                 listener.onConnectionState(ConnectionState.Pairing, "Aceite o controle Libre Remote na TV Samsung")
@@ -429,25 +431,29 @@ class SamsungTizenBackend(
         socket = null
     }
 
-    private fun verifyOrStoreCertificate(response: Response): Boolean {
-        val current = device ?: return false
-        val encoded = response.handshake?.peerCertificates?.firstOrNull()?.encoded ?: return true
-        val digest = MessageDigest.getInstance("SHA-256")
-            .digest(encoded)
-            .joinToString("") { "%02x".format(it) }
-        val key = "cert_${current.stableId}"
-        val saved = securePreferences.getString(key, null)
-        return if (saved == null) {
-            securePreferences.edit().putString(key, digest).apply()
-            true
-        } else saved == digest
-    }
-
     private fun createTlsClient(): OkHttpClient {
         val trustManager = object : X509TrustManager {
             override fun getAcceptedIssuers(): Array<X509Certificate> = emptyArray()
             override fun checkClientTrusted(chain: Array<out X509Certificate>?, authType: String?) = Unit
-            override fun checkServerTrusted(chain: Array<out X509Certificate>?, authType: String?) = Unit
+
+            override fun checkServerTrusted(chain: Array<out X509Certificate>?, authType: String?) {
+                val current = device ?: throw CertificateException("TV Samsung não selecionada")
+                val certificate = chain?.firstOrNull() ?: throw CertificateException("Certificado Samsung ausente")
+                val digest = MessageDigest.getInstance("SHA-256")
+                    .digest(certificate.encoded)
+                    .joinToString("") { "%02x".format(it) }
+                val key = "cert_${current.stableId}"
+                val saved = securePreferences.getString(key, null)
+                when {
+                    saved == null -> {
+                        if (!securePreferences.edit().putString(key, digest).commit()) {
+                            throw CertificateException("Não foi possível guardar a identidade da TV")
+                        }
+                    }
+                    saved != digest -> throw CertificateException("A identidade segura da TV Samsung mudou")
+                }
+            }
         }
         val sslContext = SSLContext.getInstance("TLS")
         sslContext.init(null, arrayOf<TrustManager>(trustManager), SecureRandom())
@@ -491,6 +497,12 @@ class DlnaMediaBackend(
     override val defaultCapabilities: Set<TvCapability> = TvCapability.dlnaMediaDefaults
 
     override fun connect(device: TvDevice, userInitiated: Boolean) {
+        val endpoints = listOfNotNull(device.avTransportUrl, device.renderingControlUrl)
+        if (endpoints.any { !NetworkAddressValidator.isLocalUrl(it) }) {
+            connected = false
+            listener.onConnectionState(ConnectionState.Error, "O serviço DLNA precisa estar na rede local")
+            return
+        }
         this.device = device
         val capabilities = device.platform.defaultCapabilities(
             hasAvTransport = device.avTransportUrl != null,
@@ -722,14 +734,26 @@ class TvBackendRegistry(
 }
 
 object NetworkAddressValidator {
+    fun isLocalUrl(raw: String): Boolean {
+        val uri = runCatching { URI(raw) }.getOrNull() ?: return false
+        if (uri.scheme?.lowercase() !in setOf("http", "https", "ws", "wss")) return false
+        if (uri.userInfo != null || uri.host.isNullOrBlank()) return false
+        return isLocalHost(uri.host)
+    }
+
     fun isLocalHost(host: String): Boolean {
-        val normalized = host.removePrefix("[").removeSuffix("]").lowercase()
+        val normalized = host.trim().removePrefix("[").removeSuffix("]").substringBefore('%').lowercase()
         if (normalized == "localhost" || normalized.endsWith(".local")) return true
         if (normalized.contains(':')) {
-            return normalized == "::1" || normalized.startsWith("fe80:") || normalized.startsWith("fc") || normalized.startsWith("fd")
+            return normalized == "::1" ||
+                normalized.startsWith("fe80:") ||
+                normalized.startsWith("fc") ||
+                normalized.startsWith("fd")
         }
-        val parts = normalized.split('.').mapNotNull(String::toIntOrNull)
-        if (parts.size != 4 || parts.any { it !in 0..255 }) return false
+        val rawParts = normalized.split('.')
+        if (rawParts.size != 4) return false
+        val parts = rawParts.map { it.toIntOrNull() ?: return false }
+        if (parts.any { it !in 0..255 }) return false
         return parts[0] == 10 ||
             (parts[0] == 192 && parts[1] == 168) ||
             (parts[0] == 172 && parts[1] in 16..31) ||
diff --git a/app/src/main/java/com/geronimo/controlelgwifi/TvDiscovery.kt b/app/src/main/java/com/geronimo/controlelgwifi/TvDiscovery.kt
index ff857ff..b87f17f 100644
--- a/app/src/main/java/com/geronimo/controlelgwifi/TvDiscovery.kt
+++ b/app/src/main/java/com/geronimo/controlelgwifi/TvDiscovery.kt
@@ -34,6 +34,7 @@ object TvDiscovery {
         val model: String?,
         val manufacturer: String?,
         val deviceType: String?,
+        val deviceUuid: String?,
         val services: List<UpnpService>,
         val raw: String
     )
@@ -59,6 +60,7 @@ object TvDiscovery {
                             platform = TvPlatform.LgWebOs,
                             supportLevel = TvSupportLevel.StableFull,
                             stableId = "${TvPlatform.LgWebOs.name}:$it",
+                            addresses = listOf(it),
                             capabilities = TvCapability.lgDefaults
                         )
                     }
@@ -86,6 +88,7 @@ object TvDiscovery {
                     platform = TvPlatform.LgWebOs,
                     supportLevel = TvSupportLevel.StableFull,
                     stableId = "${TvPlatform.LgWebOs.name}:$host",
+                    addresses = listOf(host),
                     capabilities = TvCapability.lgDefaults
                 )
                 samsung.await() -> TvDevice(
@@ -95,6 +98,7 @@ object TvDiscovery {
                     platform = TvPlatform.SamsungTizenLocal,
                     supportLevel = TvSupportLevel.Experimental,
                     stableId = "${TvPlatform.SamsungTizenLocal.name}:$host",
+                    addresses = listOf(host),
                     capabilities = TvCapability.samsungDefaults
                 )
                 else -> TvDevice(
@@ -103,6 +107,7 @@ object TvDiscovery {
                     platform = TvPlatform.Unknown,
                     supportLevel = TvSupportLevel.Unsupported,
                     stableId = "${TvPlatform.Unknown.name}:$host",
+                    addresses = listOf(host),
                     capabilities = emptySet()
                 )
             }
@@ -122,6 +127,8 @@ object TvDiscovery {
         }.getOrNull()
         val port3000 = device?.takeIf { it.platform == TvPlatform.LgWebOs }?.ip?.let { canConnect(it, 3000, 650) }
         val port3001 = device?.takeIf { it.platform == TvPlatform.LgWebOs }?.ip?.let { canConnect(it, 3001, 650) }
+        val port8001 = device?.takeIf { it.platform == TvPlatform.SamsungTizenLocal }?.ip?.let { canConnect(it, 8001, 650) }
+        val port8002 = device?.takeIf { it.platform == TvPlatform.SamsungTizenLocal }?.ip?.let { canConnect(it, 8002, 650) }
         val reachable = device?.let { isTvReachable(it, 650) }
         val backendSummary = device?.let {
             when (it.platform) {
@@ -139,23 +146,28 @@ object TvDiscovery {
         }
         NetworkDiagnostic(
             wifiConnected = wifiConnected,
-            multicastAvailable = null,
+            multicastAvailable = wifiConnected,
             localAddress = localAddress,
             tvReachable = reachable,
             port3000Reachable = port3000,
             port3001Reachable = port3001,
+            port8001Reachable = port8001,
+            port8002Reachable = port8002,
             backendSummary = backendSummary,
             summary = summary
         )
     }
 
-    fun isTvReachable(device: TvDevice, timeoutMs: Int = 500): Boolean = when (device.platform) {
-        TvPlatform.LgWebOs -> canConnect(device.ip, 3000, timeoutMs) || canConnect(device.ip, 3001, timeoutMs)
-        TvPlatform.SamsungTizenLocal -> canConnect(device.ip, 8001, timeoutMs) || canConnect(device.ip, 8002, timeoutMs)
-        TvPlatform.DlnaMedia -> listOfNotNull(device.avTransportUrl, device.renderingControlUrl, device.descriptionUrl)
-            .any { canReachHttp(it, timeoutMs) }
-        TvPlatform.RokuBlockedByPolicy -> canConnect(device.ip, 8060, timeoutMs)
-        else -> canConnect(device.ip, 80, timeoutMs) || canConnect(device.ip, 443, timeoutMs)
+    fun isTvReachable(device: TvDevice, timeoutMs: Int = 500): Boolean {
+        val hosts = (listOf(device.ip) + device.addresses).filter(String::isNotBlank).distinct().take(8)
+        return when (device.platform) {
+            TvPlatform.LgWebOs -> hosts.any { canConnect(it, 3000, timeoutMs) || canConnect(it, 3001, timeoutMs) }
+            TvPlatform.SamsungTizenLocal -> hosts.any { canConnect(it, 8001, timeoutMs) || canConnect(it, 8002, timeoutMs) }
+            TvPlatform.DlnaMedia -> listOfNotNull(device.avTransportUrl, device.renderingControlUrl, device.descriptionUrl)
+                .any { canReachHttp(it, timeoutMs) }
+            TvPlatform.RokuBlockedByPolicy -> hosts.any { canConnect(it, 8060, timeoutMs) }
+            else -> hosts.any { canConnect(it, 80, timeoutMs) || canConnect(it, 443, timeoutMs) }
+        }
     }
 
     private fun discoverSsdp(context: Context, durationMs: Long): List<TvDevice> {
@@ -258,6 +270,8 @@ object TvDiscovery {
                 ?: headers["server"]?.takeIf { it.length <= 80 }
                 ?: fallbackName
         )
+        val uuid = description?.deviceUuid?.normalizeDeviceUuid()
+        val stableId = uuid?.let { "${platform.name}:$it" } ?: "${platform.name}:$ip"
         return TvDevice(
             ip = ip,
             name = name,
@@ -265,7 +279,9 @@ object TvDiscovery {
             manufacturer = description?.manufacturer,
             platform = platform,
             supportLevel = support,
-            stableId = "$platform:$ip",
+            stableId = stableId,
+            deviceUuid = uuid,
+            addresses = listOf(ip),
             descriptionUrl = location,
             avTransportUrl = avTransport,
             renderingControlUrl = rendering,
@@ -290,20 +306,31 @@ object TvDiscovery {
             TvPlatform.RokuBlockedByPolicy to 10,
             TvPlatform.Unknown to 0
         )
-        return all.groupBy(TvDevice::ip).map { (_, variants) ->
+        return all.groupBy { device -> device.deviceUuid?.let { "uuid:$it" } ?: "ip:${device.ip}" }.map { (_, variants) ->
             val chosen = variants.maxByOrNull { priorities[it.platform] ?: 0 } ?: variants.first()
-            val saved = knownDevices.firstOrNull { it.stableId == chosen.stableId }
-                ?: knownDevices.firstOrNull { it.ip == chosen.ip && it.platform == chosen.platform }
+            val variantAddresses = variants.flatMap { listOf(it.ip) + it.addresses }.filter(String::isNotBlank).distinct()
+            val saved = knownDevices.firstOrNull { known ->
+                known.stableId == chosen.stableId ||
+                    (chosen.deviceUuid != null && known.deviceUuid == chosen.deviceUuid) ||
+                    known.addresses.any(variantAddresses::contains) ||
+                    known.ip in variantAddresses
+            }
             chosen.copy(
+                ip = variantAddresses.firstOrNull { address -> isTvReachable(chosen.copy(ip = address, addresses = listOf(address)), 220) }
+                    ?: chosen.ip,
                 name = saved?.name?.takeIf { it.isNotBlank() && it !in setOf("LG webOS TV", "Smart TV") } ?: chosen.name,
                 room = saved?.room.orEmpty(),
                 mac = saved?.mac,
                 stableId = saved?.stableId ?: chosen.stableId,
+                deviceUuid = chosen.deviceUuid ?: saved?.deviceUuid,
+                addresses = (variantAddresses + saved?.addresses.orEmpty()).distinct().take(8),
+                favorite = saved?.favorite ?: false,
                 capabilities = if (chosen.capabilities.isNotEmpty()) chosen.capabilities else saved?.capabilities.orEmpty(),
                 lastSeenAt = System.currentTimeMillis()
             )
         }.sortedWith(
-            compareByDescending<TvDevice> { knownDevices.any { saved -> saved.stableId == it.stableId } }
+            compareByDescending<TvDevice> { it.favorite }
+                .thenByDescending { knownDevices.any { saved -> saved.stableId == it.stableId } }
                 .thenByDescending { priorities[it.platform] ?: 0 }
                 .thenBy(TvDevice::displayName)
         )
@@ -328,6 +355,7 @@ object TvDiscovery {
             val model = tag(xml, "modelName")
             val manufacturer = tag(xml, "manufacturer")
             val deviceType = tag(xml, "deviceType")
+            val deviceUuid = tag(xml, "UDN") ?: tag(xml, "udn") ?: tag(xml, "uuid")
             val services = Regex("<service>(.*?)</service>", setOf(RegexOption.IGNORE_CASE, RegexOption.DOT_MATCHES_ALL))
                 .findAll(xml)
                 .mapNotNull { block ->
@@ -337,7 +365,7 @@ object TvDiscovery {
                     UpnpService(type, control)
                 }
                 .toList()
-            DeviceDescription(friendly, model, manufacturer, deviceType, services, xml)
+            DeviceDescription(friendly, model, manufacturer, deviceType, deviceUuid, services, xml)
         }
     }.getOrNull()
 
@@ -385,6 +413,13 @@ object TvDiscovery {
         .trim()
         .take(64)
 
+
+    private fun String.normalizeDeviceUuid(): String = trim()
+        .removePrefix("uuid:")
+        .lowercase(Locale.ROOT)
+        .replace(Regex("[^a-z0-9-]"), "")
+        .take(96)
+
     private fun String.decodeXml(): String = replace("&amp;", "&")
         .replace("&lt;", "<")
         .replace("&gt;", ">")
